services:
  - type: web
    name: sap-cpi-ai-studio
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: python3 backend/server.py
    envVars:
      - key: PORT
        value: 10000
